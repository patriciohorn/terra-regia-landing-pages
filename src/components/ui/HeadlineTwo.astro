---
import { cn } from "../../lib/utils";

const { label, class: className } = Astro.props;
---

<h2
  class={cn(
    `text-4xl sm:text-5xl leading-[1.1] font-regular text-neutral-950`,
    className,
  )}
>
  {label}
</h2>
