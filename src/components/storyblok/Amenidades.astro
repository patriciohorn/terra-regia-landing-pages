---
import { storyblokEditable } from "@storyblok/astro";
const { blok } = Astro.props;
---

<section {...storyblokEditable(blok)} class="py-16 sm:py-28">
  <div class="grid sm:grid-cols-2 gap-12 sm:gap-20">
    <div class="flex flex-wrap gap-x-"></div>
  </div>
</section>
